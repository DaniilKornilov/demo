schemaVersion: 2.2.0
metadata:
  name: kotlin-springboot
  version: 1.0.0
  attributes:
    alpha.dockerimage-port: 8081
  displayName: Spring Boot®
  description: Spring Boot® using Maven
  icon: https://raw.githubusercontent.com/devfile-samples/devfile-stack-icons/main/spring.svg
  tags:
    - Kotlin
    - Spring
  projectType: springboot
  language: Kotlin
  provider: Red Hat
parent:
  id: java-springboot
  registryUrl: 'https://registry.devfile.io'
components:
  - name: image-build
    image:
      imageName: daniilkornilov/demo-app:1.0.0
      dockerfile:
        uri: docker/Dockerfile
        buildContext: .
        rootRequired: false
  - name: kubernetes-deploy-mongo
    attributes:
      deployment/replicas: 1
      deployment/cpuRequest: 10m
      deployment/memoryRequest: 500Mi
      deployment/container-port: 27017
    kubernetes:
      uri: mongo.yaml
      endpoints:
        - name: mongo
          targetPort: 27017
          path: /
  - name: kubernetes-deploy-app
    attributes:
      deployment/replicas: 1
      deployment/cpuRequest: 10m
      deployment/memoryRequest: 180Mi
      deployment/container-port: 8081
    kubernetes:
      uri: app.yaml
      endpoints:
        - name: app
          targetPort: 8081
          path: /
commands:
  - id: build-image
    apply:
      component: image-build
  - id: deploy-mongo
    apply:
      component: kubernetes-deploy-mongo
  - id: deploy-app
    apply:
      component: kubernetes-deploy-app
  - id: deploy
    composite:
      commands:
        - build-image
        - deploy-mongo
        - deploy-app
      group:
        kind: deploy
        isDefault: true
